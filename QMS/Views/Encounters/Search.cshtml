@model IEnumerable<Encounter>

@{
    ViewBag.Title = "Search";
}

<form method="post">
    <div class="container-fluid">
        @Html.Partial("_SearchPartial")
        @if (Model.Count() > 0)
        {
            @Html.Partial("_ListEncountersPartial", Model)
        }
    </div>
</form>